apiVersion: v1
kind: Service
metadata:
  name: {{ include "ashta-lakshmi.fullname" . }}
  labels:
    {{- include "ashta-lakshmi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ashta-lakshmi.selectorLabels" . | nindent 4 }}
